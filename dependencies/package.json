{
	"scripts": {
		"prebuild": "npm ci --no-fund --no-audit",
		"build": "npm run build:dev && npm run build:prod",
		"build:prod": "node ../cli/index.js build",
		"build:dev": "node ../cli/index.js build --dev"
	},
	"devDependencies": {
		"@lastui/dependencies": "1.2.0"
	}
}
